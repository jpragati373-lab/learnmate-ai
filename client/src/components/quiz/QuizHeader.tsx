import type { QuizQuestion } from '../../types'
export function QuizHeader({ question }: { question: QuizQuestion }) { return <div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-indigo-50 px-2.5 py-1 text-xs font-semibold text-indigo-700">{question.topic}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold capitalize text-slate-600">{question.difficulty}</span>{question.source === 'demo' && <span className="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-700">Demo question</span>}</div> }
