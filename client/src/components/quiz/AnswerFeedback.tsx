import { CheckCircle2, XCircle } from 'lucide-react'
import type { QuizAnswerResult } from '../../services/quizService'
import { Card } from '../ui/Card'
export function AnswerFeedback({ result, topic }: { result: QuizAnswerResult; topic: string }) { return <Card className={result.correct ? 'border-emerald-100 bg-emerald-50/70' : 'border-amber-100 bg-amber-50/70'}><div className="flex items-start gap-3">{result.correct ? <CheckCircle2 className="mt-0.5 h-5 w-5 text-emerald-600" /> : <XCircle className="mt-0.5 h-5 w-5 text-amber-600" />}<div><h3 className={`font-bold ${result.correct ? 'text-emerald-800' : 'text-amber-800'}`}>{result.correct ? 'Correct' : 'Incorrect'}</h3><p className="mt-2 text-sm leading-6 text-slate-700"><strong>Explanation:</strong> {result.explanation}</p>{!result.correct && <><p className="mt-2 text-sm leading-6 text-slate-700"><strong>Correct answer:</strong> {result.correctAnswer}</p><p className="mt-2 text-sm leading-6 text-slate-700"><strong>Why your answer was incorrect:</strong> {result.misconception}</p><a href={`/assistant?subject=Computer%20Networks&topic=${encodeURIComponent(topic)}&level=beginner&learningStyle=step-by-step&accuracy=48`} className="mt-4 inline-flex rounded-xl border border-amber-300 bg-white px-3 py-2 text-sm font-semibold text-amber-800 focus:outline-none focus:ring-2 focus:ring-amber-500">Still confused? Ask AI to explain</a></>}</div></div></Card> }
