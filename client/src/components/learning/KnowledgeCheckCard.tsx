import { useState } from 'react'
import { Card } from '../ui/Card'
export function KnowledgeCheckCard({ question, answer, index }: { question: string; answer: string; index: number }) { const [revealed, setRevealed] = useState(false); return <Card><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">Question {index}</p><h3 className="mt-2 font-semibold leading-6 text-slate-900">{question}</h3>{revealed ? <p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm leading-6 text-emerald-800"><strong>Answer:</strong> {answer}</p> : <button type="button" onClick={() => setRevealed(true)} className="mt-4 text-sm font-semibold text-indigo-600 underline-offset-4 hover:underline focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">Reveal Answer</button>}</Card> }
